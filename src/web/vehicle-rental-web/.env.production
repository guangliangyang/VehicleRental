# Production environment configuration for Container Instance
# Container Instance URL (Azure Container Instance with public IP)
REACT_APP_API_URL=http://vehicle-rental-api-dev.eastus.azurecontainer.io:8080

# Note: REACT_APP_API_KEY is not set because Container Instance doesn't require authentication
# This simplifies frontend integration and direct container access
